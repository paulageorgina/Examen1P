import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'examen_cardenas_paula',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
